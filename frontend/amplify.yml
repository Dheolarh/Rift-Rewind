version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build"
    build:
      commands:
        - cd frontend && npm ci && npm run build
  artifacts:
    baseDirectory: frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*